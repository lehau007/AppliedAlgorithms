#include <iostream>
#include <vector>
#include <string>
#include <climits>
#include <algorithm>

using namespace std;

int csize, asize, bsize;
string a, b, c;

vector<bool> memo[]
bool solve(int l_a, int l_b) {

}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    cin >> a >> b >> c;

    if (solve(0, 0, 0))
        cout << "T";
    else
        cout << "F";
    return 0;
}
